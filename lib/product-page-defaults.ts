/** Fallback copy when `product_page_copy` rows are null or blank (shipping / returns). */
export const DEFAULT_PDP_SHIPPING_DELIVERY =
  "Standard delivery typically takes 3-5 business days. Express shipping options are available at checkout. We currently offer shipping across the country with tracking provided for all orders.";

export const DEFAULT_PDP_RETURNS =
  "We accept returns within 14 days of the delivery date. Items must be unworn, unwashed, and in their original condition with all tags attached. Please note that sale items are final and cannot be returned or exchanged.";
